============
Installation
============

At the command line::

    $ pip install ceilometerdemo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ceilometerdemo
    $ pip install ceilometerdemo
